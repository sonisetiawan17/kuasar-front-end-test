import '../styles/logo.css';

const Logo = () => {
  return (
    <div className="logo">
      <h1>
        List <span className="logo_secondary">Countries</span>
      </h1>
    </div>
  );
};

export default Logo;
